import base
import logging
import os
import sys
import time
import traceback

#write a function that usese the get_all_data function to create a list of lists with ditonaries inside
#then writes then tranfers all dictonaries form the list of lists to a new list
#uses remaove status to remove all statuses form the list
#converts the list of dictonaries to a dataframe 
#appends the dataframe to the given sql databese

